name: game-studios-pygame

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
      - api

  web:
    build:
      context: ../apps/playground-web
      dockerfile: Dockerfile
    environment:
      - API_SERVICE_URL=http://api:8000
    depends_on:
      - api

  api:
    build:
      context: ../apps/playground-api
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=sqlite:////data/games.db
      - GAMES_DIR=/data/games
      - SCREENSHOTS_DIR=/data/screenshots
      - RUNNER_IMAGE=pygame-runner:latest
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    volumes:
      - game-data:/data
      - /var/run/docker.sock:/var/run/docker.sock
      - ../runtime/pygame-sdk:/sdk:ro
    depends_on:
      runner-build:
        condition: service_completed_successfully

  runner-build:
    build:
      context: ../docker/game-runner
      dockerfile: Dockerfile
    image: pygame-runner:latest
    entrypoint: ["echo"]
    command: ["image built"]

volumes:
  game-data:
